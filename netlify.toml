[build]
  base    = "/"                 # Your project root
  publish = "dist"              # Folder generated by npm run build
  command = "npx vite build"     # Build command
  functions = "netlify/functions"  # Serverless functions folder

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
